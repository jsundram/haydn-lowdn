.DS_Store

# Local helper scripts (headless screenshot/PDF runs)
.claude_*.sh

# Probe artifacts from local verification
test_*.html
probe*.png

# Hand-generated exports from the in-app buttons (regenerate locally if wanted)
haydn-extremes-*.png
Haydn String Quartets*.pdf

# snapshot.sh's temporary copy of index.html (the outputs/ dir IS tracked)
.snapshot_*.html
